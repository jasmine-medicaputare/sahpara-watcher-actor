{
  "title": "SAHPRA Watcher Input",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "sources": {
      "title": "Sources to Scrape",
      "type": "array",
      "description": "Select which SAHPRA pages to monitor. Leave empty to monitor all.",
      "editor": "select",
      "enum": [
        "safety_alerts",
        "product_recalls",
        "news",
        "industry_comm",
        "documents_for_comment",
        "safety_info"
      ],
      "prefill": ["safety_alerts", "product_recalls", "news", "industry_comm", "documents_for_comment", "safety_info"],
      "uniqueItems": true
    },
    "max_items": {
      "title": "Max Items Per Source",
      "type": "integer",
      "description": "Maximum number of items to scrape per source page.",
      "default": 25,
      "minimum": 1,
      "maximum": 100
    },
    "proxy": {
      "title": "Proxy Configuration",
      "type": "object",
      "description": "Proxy settings for scraping SAHPRA pages.",
      "default": {
        "useApifyProxy": false
      },
      "properties": {
        "useApifyProxy": {
          "title": "Use Apify Proxy",
          "type": "boolean",
          "description": "Whether to use Apify proxy for requests.",
          "default": false
        }
      }
    }
  }
}
